import type { ReactNode } from "react";

export const metadata = {
  title: "مباشر من المركز — AMILCAR Auto Care",
};

export default function LiveLayout({ children }: { children: ReactNode }) {
  return children;
}
